<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/insert_menu"
        android:icon="@drawable/ic_add_deal"
        android:title="@string/new_travel_deal"
        app:showAsAction="never" />

    <item android:id="@+id/logout_menu"
        android:title="@string/logout_menu"
        app:showAsAction="never" />

    <item
        android:id="@+id/refresh_menu"
        android:icon="@drawable/ic_refresh"
        android:title="@string/refresh_menu"
        app:showAsAction="ifRoom" />

</menu>